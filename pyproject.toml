[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "gandalf-csr"
version = "1.0.2"
description = "Fast path finding in large knowledge graphs"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Max Wang", email = "max@covar.com"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "bmt>=1.4.8",
    "lmdb>=1.4.0",
    "msgpack>=1.0.0",
    "numpy>=1.20.0",
    # The Translator-wide TRAPI object model: request models and the
    # OpenAPI schema come from here, and its TypedDicts type gandalf's
    # internals.  Requires Python 3.10, hence the floor above.
    "translator_tom>=2.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=3.0",
    "black>=22.0",
    "mypy>=1.0",
    "types-PyYAML>=6.0.12"
]
server = [
    "fastapi>=0.100.0",
    "httpx>=0.24.0",
    "orjson>=3.9.0",
    "psutil>=5.9.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.12.0",
    "uvicorn>=0.20.0",
    "zstandard>=0.22.0",
]
mongo = [
    "pymongo>=4.0",
]

[project.scripts]
gandalf-build = "scripts.build_graph:main"
gandalf-build-mongo = "scripts.build_graph_mongo:main"
gandalf-query = "scripts.query_paths:main"
gandalf-diagnose = "scripts.diagnose:main"

[tool.mypy]
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
